# 10 · 작업 분할

**티켓은 CY-646 하나다.** 아래 단위로 나눠 각각 독립 커밋한다.
다른 세션이 하나만 집어서 cold 로 시작할 수 있게 범위와 완료 조건을 닫아 두었다.

## 전제 — v2 발급 경로는 아직 없다

`EngineVersion.V2` 는 정합성·관측 축에만 쓰인다. 발급은 `CouponIssueService`(v1) 하나뿐이고
`infra/redis` 에도 발급 코드가 없다. **이 작업은 수정이 아니라 신규 구현이다.**

## 의존 그래프

```
S1 값 codec
 └ S2 Lua 4종
     └ S3 게이트 포트·어댑터
         ├ S4 발급 유스케이스 ─ S5 응답·지표 ─ S9 경합 테스트
         ├ S6 재고 복원 연결        (S4 와 병렬)
         ├ S7 PENDING 계측          (병렬)
         └ S8 재구성·재동기화 + 락   (병렬)
                                        └ S10 문서 정합·완료 확인
```

## 단위

| # | 범위 | 모듈 | 선행 | 완료 조건 | 문서 |
|---|---|---|---|---|---|
| **S1** | 값 codec(encode/decode) · 요청토큰 생성기 | core | — | round-trip 테스트 green. `\|` 포함 키·접두 충돌 사례 포함 | 01 |
| **S2** | Lua 4종 — 선점 · 완료 CAS · 보상 CAS · 배치 복원. 반환 코드 상수. **Java·Lua 교차 판정 테이블**(08 하단) | infra/redis | S1 | Lua 계약 테스트 green. 다른 토큰으로 완료·보상 시도가 상태를 안 바꿈. 교차 판정 5종이 양쪽 같은 답 | 01 02 03 06 08 |
| **S3** | 게이트 포트 + Redis 어댑터. `meta` 읽기·쓰기 포함 | core port · infra/redis | S2 | 어댑터 통합 테스트 green. 키 이름·해시태그 `{r}` 고정 | 02 |
| **S4** | v2 발급 유스케이스 — Lua → 단일 TX → 완료 CAS. 예외 시 §4.9 판단. **회차 단위 v1/v2 배타 토글**. 요청토큰 생성기를 api 인스턴스당 하나의 빈으로 조립 | core | S3 | 정상·실패 경로 통합 테스트. v1 과 동시 실행 불가. 재시작 전·후 토큰 충돌 없음 | 01 02 03 |
| **S5** | 반환 코드 → HTTP 매핑 · 카운터 3종 · 이상 카운터 4종 | api · core | S4 | `-4`/`-6`/`-7` 이 서로 다른 응답. 음수 반환을 삼키는 경로 없음 | 04 |
| **S6** | 취소·사용취소·만료의 `afterCommit` 복원 연결 | core | S3 | 단건·배치 경로 각각 테스트. Redis 실패를 전파하지 않음 | 06 |
| **S7** | PENDING 계측 — `HSCAN` 커서 · `stalePendingCount` | batch | S3 | 게이지가 나온다. **자동 회수 코드가 없다** | 05 |
| **S8** | 재구성·재동기화 절차 + 동시 실행 차단 | batch | S3 | 절차 5단계 + 4′. batch 단독 소유 또는 락 | 07 |
| **S9** | 경합 테스트 C9~C12 + 래치 훅 | test | S4 S5 | C9~C12 green. 훅이 프로덕션 hot path 에 분기를 안 남김 | 08 |
| **S10** | 문서 정합 · 통합 확인 | docs | 전부 | gap 4축 0 · 이상 카운터 0 · 09 체크리스트 전부 참 | 09 |

## 커밋

```
feat(CY-646): <한 줄>      ← S1~S8
test(CY-646): <한 줄>      ← S9
docs(CY-646): <한 줄>      ← S10
```

**단위 하나 = 커밋 하나.** 섞지 않는다. 되돌릴 때 이게 전부다.

## 병렬로 돌릴 때

S3 가 끝나면 S4 · S6 · S7 · S8 이 서로를 안 기다린다.
다만 **S4 와 S6 은 같은 core 모듈**이라 파일 충돌 가능성이 있다 — 먼저 잡는 쪽이 알린다.

## 세션 하나가 시작할 때 읽을 것

```
00 이정표 → 10 작업분할(이 파일) → 자기 단위의 "문서" 열 → 09 완료 기준
```

## 빌드 범위

전체 build 를 돌리지 않는다. **관여 모듈만.**
S1 core · S2 S3 infra/redis · S4 S6 core · S5 api · S7 S8 batch.
